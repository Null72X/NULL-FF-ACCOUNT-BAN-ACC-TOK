For every folder must I install requirements.txt each and make sure u have python installed from Microsoft Store Or Elsewhere.
Credit Watashii 
Visit My Server:https://guns.lol/Watashii

Just Run Main Script Python See Vercel.json Every File Specified.
pip install -r requirements.txt (Must)
pip install protobuf (Optional For Syntax Bugs)
pip uninstall protobuf (Optional For Syntax Bugs)
python api.py

/get post for

Environment URL Pattern
Local http://localhost:5000/ban?access_token={token}
Local Network http://{your-ip}:5000/ban?access_token={token}
Vercel (Dev) https://project-name.vercel.app/ban?access_token={token}
Vercel (Prod) https://custom-domain.com/ban?access_token={token}
ngrok https://{random}.ngrok.io/ban?access_token={token}